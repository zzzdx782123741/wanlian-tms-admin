# 开发环境默认绑定到 wanlian-tms-api
VITE_API_URL=/api
VITE_API_PROTOCOL=http
VITE_API_HOST=localhost
VITE_API_PORT=3000

# 如需直接覆盖完整后端地址，可放开并按需配置
# VITE_API_PROXY_TARGET=http://localhost:3000

# 默认关闭快捷测试登录入口，需要时手动改为 true
VITE_ENABLE_TEST_LOGIN=false
